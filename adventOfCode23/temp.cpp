#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main() {
    string input;
    while (cin >> input) {
        
    }

    /*
    ifstream inputFile("input.txt");
    if (inputFile.is_open()) {
        string line;
        while (getline(inputFile, line)) {
            cout << line << endl;
        }
        inputFile.close();
    }
    */
    return 0;
}